# Copy to .env (never commit .env). Values pre-fill the sidebar fields.
N8N_WEBHOOK_URL=https://<your-subdomain>.app.n8n.cloud/webhook/fallguard
# Same value as the Header Auth credential (name X-FallGuard-Key) on the Webhook node
N8N_WEBHOOK_KEY=
