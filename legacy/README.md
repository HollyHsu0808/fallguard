# Legacy: v1 single-file app

`app_v1.py` is the original prototype (detector + pose rules + state machine + in-app Telegram photo alerts). It is kept for reference; the current app lives in `../app/` and sends events to n8n instead of alerting by itself.
