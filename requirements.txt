# Versions the app was last run with (Python 3.13, CPU only).
# ultralytics installs torch and torchvision itself; a CPU build of torch is enough.
streamlit==1.58.0
ultralytics==8.4.56
opencv-python>=4.13.0,<5
numpy==2.2.6
requests==2.32.3
