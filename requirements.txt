# Python 3.10+ (tested on 3.13, CPU only). ultralytics installs torch itself; a CPU build is enough.
streamlit>=1.38
ultralytics>=8.3
opencv-python>=4.10,<5
numpy>=1.26
pandas>=2.0
requests>=2.31
python-dotenv>=1.0
imageio-ffmpeg>=0.5     # bundled ffmpeg: H.264 fall clips that play inline in Telegram
